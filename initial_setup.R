### Run this code before running any of the files. This should only need to be done a single time
### on initial setup.

install.packages("knitr")
install.packages("markdown")
install.packages("rmarkdown")
install.packages("dplyr")